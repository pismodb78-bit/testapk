// Корневой build-файл.
//
// Версии плагинов переехали в settings.gradle.kts, в блок pluginManagement:
// объявление здесь заставляло Gradle разрешать андроидный плагин даже при
// сборке одной только настольной версии — см. пояснение там же.
